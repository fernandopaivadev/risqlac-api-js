{
  "ignore": [
    "**/*.test.ts",
    "**/*.spec.ts",
    "node_modules"
  ],
  "watch": [
    "src"
  ],
  "exec": "npx prisma migrate deploy && npx prisma generate && npm run build && node ./dist/index.js",
  "ext": "ts"
}
